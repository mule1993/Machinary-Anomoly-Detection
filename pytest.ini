[pytest]
minversion = 7.0
addopts = -ra -q --strict-markers
testpaths = tests
pythonpath = .

markers =
    unit: Fast, isolated unit tests.
    data: Data schema and quality validation tests.
    integration: Slow end-to-end pipeline or training tests.