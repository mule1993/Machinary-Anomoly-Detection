# 1. Ignore the files inside data, but DO NOT block the folder itself
/data/*
/data/**/*

# 2. EXCEPTION: Force Git to track any .dvc pointer files
!/data/**/*.dvc
!*.dvc

# 3. Global file patterns to block large binaries elsewhere
*.csv
*.parquet
*.tar.gz
*.joblib
*.pkl

# Python environment & orchestration files
.venv/
__pycache__/
.env
.astro/